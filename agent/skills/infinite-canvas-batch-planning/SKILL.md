---
name: infinite-canvas-batch-planning
description: 使用当前 Canvas Tools 规划和执行批量画布操作。
---

# Canvas 批量规划（当前阶段）

- 先查询任务范围并将需求拆成尽可能少的结构化工具调用。
- 高成本、参数不完整或大范围任务先进行一次简短确认。
- 用户明确“直接运行/使用默认”时，使用当前画布默认值执行。
- 当前尚未实现独立任务节点；不声称任务已交给子 Agent 或可在后台自主推进多阶段 DAG。
- 只使用现有 Canvas Tools 能力，并对实际提交数量和结果如实回报。
